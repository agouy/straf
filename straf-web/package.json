{
  "name": "straf-web",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "STR Analysis for Forensics — pure-browser reimplementation",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "parity:dump": "node --import tsx parity/dump_ts.ts",
    "parity:compare": "node --import tsx parity/compare.ts"
  },
  "dependencies": {
    "@observablehq/plot": "^0.6.17",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
